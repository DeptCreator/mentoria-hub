/*
  MENTORIA HUB - DATABASE SETUP
  
  Step 1: Go to https://supabase.com/dashboard/project/lczyxcleqedjvrodxlsm/sql/new
  
  Step 2: Open supabase/migrations/001_initial_schema.sql from the project
          and paste its ENTIRE content into the SQL Editor.
          
  Step 3: Click "Run" or press Cmd+Enter
  
  Step 4: Open supabase/migrations/002_seed_data.sql from the project
          and paste its ENTIRE content into the SQL Editor.
          
  Step 5: Click "Run" or press Cmd+Enter
  
  Step 6: Verify by running:
          SELECT * FROM courses; -- should show 5 courses
          SELECT * FROM opportunities; -- should show 10 opportunities
          SELECT * FROM categories; -- should show 8 categories
  
  That's it! The database is ready.
  
  To make your account admin (so you can access the admin panel):
  Run: UPDATE profiles SET is_admin = TRUE WHERE email = 'your@email.com';
*/
